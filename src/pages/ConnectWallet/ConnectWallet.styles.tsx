import { createUseStyles } from 'react-jss'

const styles = {
	page: {
		minHeight: '100vh',
		minWidth: '100vw',
		display: 'flex',
		alignItems: 'center',
		justifyContent: 'center',
	},
}

export default createUseStyles(styles)
